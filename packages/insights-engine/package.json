{
  "name": "@smart-sneaker/insights-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Shared segmentation + insight computation, sport-profile-driven. Imported by both the client app and the cloud session worker.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@smart-sneaker/data-contracts": "*"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
