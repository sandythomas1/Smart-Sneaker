{
  "name": "@smart-sneaker/session-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Run session worker: consumes session-processing events, recomputes insights server-side (authoritative result), records client/cloud consistency",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@smart-sneaker/data-contracts": "*",
    "@smart-sneaker/insights-engine": "*",
    "fastify": "^5.0.0",
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
