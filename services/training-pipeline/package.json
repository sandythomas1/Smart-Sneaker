{
  "name": "@smart-sneaker/training-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Run training service: freezes labeled dataset snapshots and runs triggered/scheduled training against them, producing versioned model artifacts + evaluation metrics",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@smart-sneaker/data-contracts": "*",
    "@smart-sneaker/dataset-store": "*",
    "@smart-sneaker/insights-engine": "*",
    "fastify": "^5.0.0",
    "firebase-admin": "^13.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
