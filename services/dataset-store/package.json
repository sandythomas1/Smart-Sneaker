{
  "name": "@smart-sneaker/dataset-store",
  "version": "0.1.0",
  "private": true,
  "description": "Labeled dataset snapshots: named, versioned, immutable training corpora built from labeled session records. Library (no server) — consumed by the training pipeline.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@smart-sneaker/data-contracts": "*",
    "firebase-admin": "^13.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
