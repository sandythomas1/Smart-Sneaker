{
  "name": "smart-sneaker",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Sneaker — sport-specific biomechanics insight platform (client app + GCP cloud)",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
